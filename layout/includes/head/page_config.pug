- var showToc = !!page.toc && toc(page.content) !== '';

script#config-diff.
    var PAGE_CONFIG = {
        is_post: !{is_post()},
        is_page: !{!is_post() && !is_home()},
        is_home: !{is_home()},
        page: '!{page.type}',
        toc: !{showToc},
        color: !{page.color ? "'"+ page.color+"'" : false},
    }
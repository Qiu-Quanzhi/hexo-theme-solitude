#about-page
  display flex
  padding-top 1rem
  flex-direction column
  align-items center
  gap .5rem

  .author-title
    font-size 2rem
    font-weight 700
    margin .5rem 0
    line-height 1

if "contentinfo" in $about
  @import "contentinfo"

if "skills" in $about
  @import "skills"

if "oneself" in $about
  @import "oneself"

if "motto" in $about
  @import "buff"
  @import "maxim"

if "game" in $about
  @import "game"

if "careers" in $about
  @import "careers"

if "likes" in $about
  @import "like"